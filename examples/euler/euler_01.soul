# Euler 01: Find the sum of all multiples of 3 or 5 below 1000.

fn main :: void
{
	let mut sum : u64 = 0;
	for(let i : u64 = 0; i < 1000; ++i)
	{
		if((i % 3 == 0) || (i % 5 == 0))
		{
			sum += i;
		}
	}

	std::print(sum);
}

